import type { CourseFixture } from '../fixture-types';

/**
 * Полный список системных курсов для накатки.
 *
 * KS-1958: пусто. Все тестовые фикстуры удалены, раздел «Уроки»
 * стартует пустой и ждёт настоящего контента.
 *
 * Новые курсы добавлять сюда по мере готовности (фикстура в
 * `./<course-slug>/index.ts` → импорт → запись в массив).
 */
export const COURSES: CourseFixture[] = [];
