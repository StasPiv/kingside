/**
 * KS-3962 / KS-3967 / ADR-119 §8 эпики A+B. Корневой экран раздела
 * «Лекции» по маршруту `/lectures`. На шаге A был заглушкой; теперь
 * содержит полноценную тренерскую страницу `MyLecturesPage`
 * (KS-3967).
 *
 * Контекст «тренер / ученик» (показывать тренерский режим целиком
 * или сначала проверять «есть ли свои лекции и переключаться на
 * ученический режим») разводится в KS-3969. Здесь — стабильная
 * точка маршрута, чтобы App.tsx не пересоединять при каждом шаге.
 */
export { MyLecturesPage as LecturesListPage } from './MyLecturesPage';
