/**
 * KS-3962 / KS-3967 / KS-3969 / ADR-119 §8 эпики A+B. Корневой
 * экран раздела «Лекции» по маршруту `/lectures`. В первой
 * итерации показывает обе части (тренерскую и ученическую) через
 * `LecturesIndexPage` (KS-3969). Эпик C сузит до контекстного
 * режима по наличию собственных лекций.
 *
 * Сохраняем именованный экспорт `LecturesListPage` стабильным,
 * чтобы App.tsx не пересоединять при каждой итерации внутреннего
 * наполнения.
 */
export { LecturesIndexPage as LecturesListPage } from './LecturesIndexPage';
