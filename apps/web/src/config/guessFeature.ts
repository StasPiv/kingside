/**
 * KS-3412 (ADR-086, guess-the-move F3). Гейт точки входа фичи «угадай
 * ход» (роут `/guess` + кнопка на ArchiveGamePage).
 *
 * Фичу нельзя выкатывать по частям (F1+F2+F3+L1) — пользователь просит
 * показать готовое целиком. Пока связка не готова, точка входа СКРЫТА в
 * проде: даже если другой задачей случайно задеплоят фронт, недоделанная
 * фича не появится. В dev — видна (для разработки/скриншотов).
 *
 * Финальный reveal (общий деплой связки) — флип на `true` (или перевод на
 * runtime feature-flag, когда backend добавит `guessEnabled` в whitelist).
 */
export const GUESS_ENTRY_ENABLED: boolean = import.meta.env.DEV;
