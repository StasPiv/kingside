/**
 * KS-2554: блок «Слабые темы» (KS-1928 / ADR-032 §3) скрыт целиком.
 * Категории/темы в UI пазлов решено пока не показывать (часть тем
 * приходит сырыми camelCase-ключами без переводов; методика выбора
 * по теме — задача на будущее). Компонент оставлен в codebase, чтобы
 * не ломать импорты в `PuzzleStatsPage`/`PuzzleMistakesPage` —
 * возвращает `null`. Вернуть UI можно одним коммитом, восстановив
 * прежнее тело из git history (последний живой коммит — KS-2496).
 */
export function MistakesDiaryBlock(): null {
  return null;
}
