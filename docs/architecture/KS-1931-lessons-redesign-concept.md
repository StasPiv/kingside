## KS-1931 — Концепция редизайна раздела «Уроки»

**Дата:** 2026-04-26 (обновлено: решения пользователя по 3 развилкам зафиксированы)
**Статус:** Согласовано пользователем, ждёт ревью координатора перед запуском задач
**Связанные:**
- [ADR-024 Lessons module](../adr/024-lessons-module.md) — фича-родитель
- [ADR-026 User courses](../adr/026-user-courses.md) — пользовательские курсы
- [ADR-030 Lobby latest+authors](../adr/030-user-courses-lobby.md) — discovery-блоки на лобби
- [ADR-031 Lessons lobby redesign](../adr/031-lessons-lobby-redesign.md) — предыдущая итерация редизайна (частично реализована)
- KS-1840 — `MyCoursesBlock`, KS-1890 — `EnrolledCoursesBlock`
- KS-1922 — `LessonsHero` (5 состояний), KS-1923 — pillar+discover, KS-1924 — lazy/skeleton

---

### 0. TL;DR

Это **вторая итерация** редизайна `/lessons`. ADR-031 разобрался с
макроструктурой (hero / personal / curriculum / discovery, выделение
`/lessons/discover`), но **не закрыл** содержательные жалобы пользователя:

- карточки курсов однотипные, без визуальных «крючков» (нет обложек,
  нет понимания сложности/длительности/ЦА);
- «текущий курс» в hero подаётся как «главный», но юзер этого не
  выбирал — несколько активных курсов равноправны;
- блок «открытие уровня Средний» (`LevelGateBanner`) живёт отдельной
  плашкой и считывается как рудимент — он показывает условия, не
  показывая прогресса к ним;
- тексты тизеров слабые / технические / отсутствуют (системный
  каталог сейчас содержит только demo-курс).

Документ — **концепция, не ADR**.

#### 0.1 Решения пользователя (зафиксировано)

Все 3 развилки концепции закрыты:

| # | Развилка | Решение |
|---|---|---|
| 1 | Hero для P1 | **3-state**: 0 активных → приветствие; 1 активный → большая «Continue»-карточка; ≥2 активных → компактный блок со ссылкой «Мои активные курсы» |
| 2 | LevelGateBanner | **Удалить полностью.** Без замены таймлайном. Семантика уровней не формализована, вернёмся отдельно. |
| 3 | Расширение DTO Course | **Полное расширение**: `coverUrl`, `difficulty`, `estimatedMinutes`, `audienceI18nKey`, `tags`. Backend принимает поля; заполнение контента — отдельная задача автора курса (через админку). |

Разбивка на задачи — см. §11.

---

### 1. Что есть сейчас (фактически)

`apps/web/src/pages/LessonsPage.tsx` рендерит блоки в следующем порядке:

```
<header>          Уроки / Структурированный курс шахмат
<LessonsHero>     Continue / Author / Start / Guest / Loading
<ReviewsDueBlock> SM-2 «к повторению сегодня»
<LevelGateBanner> «Чтобы открыть уровень Средний» (только для beginner)
<MyCoursesBlock>  «Мои курсы» (свои авторские) + CTA «+ Создать»
<EnrolledCoursesBlock> «Курсы, которые я прохожу» (чужие)
<CurriculumPillarBlock> Beginner → Intermediate → Advanced
<CommunityStripBlock>   Compact strip последних community-курсов
```

Что у каждой карточки курса в текущей вёрстке (`CurriculumPillarBlock`,
`EnrolledCoursesBlock`, `MyCoursesBlock`):

- **Заголовок** курса (`titleI18nKey`).
- **Описание** одной строкой (`descriptionI18nKey`) — у системных
  ключей сейчас либо пусто, либо «Технический курс для dev/QA».
- **Счётчик** «N уроков» / «K/N уроков пройдено».
- **Бейдж** «Рекомендуем», «✓ Пройден», «Public/Private».

Чего у карточки **нет** ни в DTO `CourseListItem`, ни в `UserEnrolledCourseDto`
(`packages/shared/src/types/lessons.ts:437`):

- **обложки** (cover image) — нет поля;
- **сложности** (внутри уровня — easy/medium/hard) — нет;
- **длительности** (часов / времени на курс) — нет;
- **целевой аудитории** («для тех, кто играет 1200+», «для детей
  6–10») — нет;
- **превью урока** (мини-доска, FEN-диаграмма) — нет;
- **автора** (для системных) — нет;
- **тегов/тем** (тактика, эндшпиль, дебют) — есть `block.key` на
  уровне урока, но не агрегируется в курс.

LevelGateBanner (`apps/web/src/components/lessons/LevelGateBanner.tsx`)
рендерится отдельной плашкой над `MyCoursesBlock` со списком блокеров
(`course_not_completed`, `puzzle_rating`, `games_played`, `rapid_rating`,
`puzzles_solved`). У плашки нет визуального индикатора прогресса
по этим метрикам — только текст вида «Сыграно партий: 3/20».

> Скрин «до» через playwright сделать не удалось — в окружении
> архитектора `playwright` не установлен. Скрины предыдущей
> итерации лежат в `/tmp/KS-1919/desktop-lessons-full.png` и
> `/tmp/KS-1919/mobile-lobby.png` (ссылки из ADR-031). Опишем
> состояние через анализ кода — этого достаточно для разговора
> о концепции; визуальные скрины «до/после» снимет фронтенд-агент
> в момент реализации.

---

### 2. Жалобы пользователя — разбор

| # | Жалоба | Источник в коде | Что не закрыто ADR-031 |
|---|---|---|---|
| 1 | «Перегружена, мало визуальной информации, информация спутана» | `LessonsPage.tsx`: 8 секций подряд | ADR-031 уменьшил длину (вынес discovery), но не добавил визуального разнообразия — секции по-прежнему похожие |
| 2 | «Однотипные тизеры курсов с непривлекательными текстами» | `CurriculumPillarBlock.tsx:141` (одинаковый `lessons-course-card`); `descriptionI18nKey` слабый | ADR-031 §4.3 предложил S/M/L размеры, но **не реализовано**; тексты в seed не пересмотрены |
| 3 | «Текущий курс» выделен — непонятно почему именно он главный | `LessonsHero.tsx:58–101` (state.kind === 'continue', берёт самый свежий по `lastActivityAt`) | Логика выбора **не показана юзеру**: он видит «Continue: <X>», но не «Ты последний раз был здесь 2 дня назад» |
| 4 | Блок «открытие уровня Средний» — рудимент | `LevelGateBanner.tsx`; rendered в `LessonsPage.tsx:113` | ADR-031 §6.1 FE-6 планировал встроить в карточку уровня — **не реализовано**, плашка живёт отдельно |

ADR-031 заложил каркас, но содержательная часть («что внутри карточки
курса», «как её сделать привлекательной») оказалась за рамками.
Эта концепция — про содержательную часть.

---

### 3. Структура страницы

Жалобы №1 и №2 («перегружена», «спутана») — про **группировку**.
Текущая группировка: «мои авторские» / «которые я прохожу» / «системный
каталог» / «community». Юзер не видит **разделения по контракту**:

- Что я **уже начал** (личный фокус, главное).
- Что я **могу пройти** (каталог системных курсов — определённый набор).
- Что мне **рекомендуется** (личная рекомендация на основе уровня).
- Что **создаёт сообщество** (discovery — необязательно).

#### 3.1 Предлагаемая 4-уровневая группировка

```
HERO                       ← 3 состояния по числу активных курсов (см. §3.2)
                             0 → приветствие  ·  1 → Continue L  ·  ≥2 → compact

L1  МОЁ ОБУЧЕНИЕ          ← все курсы с прогрессом (если ≥1)
                             если ≥2 активных — Hero compact уводит на
                             отдельную /lessons/my-active; здесь дублировать
                             не нужно. Если 1 активный — он в Hero, L1 не
                             рендерится. Если все курсы пройдены —
                             компактная плашка «✓ Все курсы пройдены».

L2  РЕКОМЕНДУЕМ ВАМ        ← персонализированный список
    На основе уровня / последних ошибок
    1–2 карточки максимум

L3  СИСТЕМНЫЙ КУРС         ← структурированный путь Kingside
    Beginner → Intermediate → Advanced (вертикально, как раньше)
    Без таймлайна-индикатора уровней (см. §5).

L4  СООБЩЕСТВО (compact)   ← strip + ссылка на /lessons/discover
```

> **Изменение к предыдущей версии концепции:** L1 «Моё обучение» как
> отдельная секция на главной — **схлопывается с Hero**. По решению
> пользователя за все активные курсы отвечает Hero (3 состояния, см.
> §3.2); полный список — на отдельной странице `/lessons/my-active`.
> Это даёт более чистую главную и решает проблему «перегружено».

#### 3.2 Hero — 3 состояния по числу активных курсов

**Решение пользователя:** `Hero` адаптируется к числу активных курсов
(курсов с `progress != null && progress.completedAt == null`).

| Состояние | Когда | Что показывает | CTA |
|---|---|---|---|
| **A. Приветствие (0 активных)** | Аутентифицирован, но прогресса нет (P2 в терминах ADR-031) | «С возвращением, @user. Начни с курса для начинающих.» + краткое описание системного курса | «Открыть курс для начинающих» → `/lessons/courses/<beginner-slug>` |
| **B. Continue L (1 активный)** | Ровно один курс с прогрессом | Большая «Continue»-карточка: title курса + последний урок + прогресс-бар + (если возможно) превью доски | «▶ Продолжить» → `/lessons/<slug>` или `/lessons/my/<slug>` |
| **C. Compact (≥2 активных)** | Два и более активных курса | Компактный блок: «У тебя N активных курсов · последняя активность M дней назад» | «Мои активные курсы →» → `/lessons/my-active` (новая страница) |

Гости (P3) и авторы (P4) — отдельные состояния, как в ADR-031 §4.1
(`guest`, `author`). Состояния A/B/C — внутри ветки «аутентифицирован,
не автор без активности».

**Почему 3-state, а не равноправный compact-список:**
- При 1 активном курсе compact-блок «У тебя 1 активный курс» избыточен —
  юзеру явно нужно вернуться именно туда. Нет конкуренции, нет проблемы
  «навязали».
- При ≥2 курсах большая карточка «Continue» по `lastActivityAt`
  действительно навязывает выбор системы — юзер мог хотеть второй курс.
  Compact-блок со ссылкой на отдельную страницу даёт юзеру **выбрать
  самому** через клик.
- При 0 — приветствие с CTA на beginner-курс закрывает сценарий
  onboarding'а.

**Новая страница `/lessons/my-active`:**
- Все активные курсы пользователя (system + custom + enrolled),
  отсортированные по `lastActivityAt` desc.
- Карточки M-размера (см. §4.2), с прогрессом и CTA «Продолжить».
- Без всего остального («Рекомендуем», «Системный курс», «Сообщество»).
- Доступна также прямой ссылкой из меню/breadcrumb.
- `breadcrumb`: `Уроки / Мои активные курсы`.

**Логика перехода между состояниями:**
- Решается на FE в `useLessonsHeroContext` (расширение существующего
  хука, добавление подсчёта активных курсов).
- Все данные уже доступны из `userCoursesApi.listEnrolled()` +
  `lessonsApi.listCourses()` (фильтр `progress.completedAt == null
  && progress != null`).
- Новых REST-эндпоинтов для Hero — не нужно. Для страницы
  `/lessons/my-active` можно реюзать те же эндпоинты на FE (агрегация
  на клиенте) либо добавить `GET /lessons/active-courses` (опционально,
  для оптимизации; на MVP — не критично).

#### 3.3 Каталог vs «мои курсы» vs «рекомендации»

| Секция | Что в ней | Откуда данные |
|---|---|---|
| Hero | Активные курсы (см. §3.2 — 3 состояния) | `userCoursesApi.listEnrolled` + `lessonsApi.listCourses` (фильтр `progress != null && completedAt == null`) |
| L2 «Рекомендуем вам» | 1–2 курса по `recommendedLevel` + анализ слабых тем (из puzzle-stats) | `lessonsApi.listCourses` + новый эндпоинт рекомендаций (можно отложить, сначала использовать `recommendedLevel`) |
| L3 «Системный курс» | Полный путь Beginner → Intermediate → Advanced; внутри уровня — компактный список курсов | `lessonsApi.listCourses` (по уровню) |
| L4 «Сообщество» | 4–6 последних community-курсов в strip | `userCoursesApi.listLatest` |
| `/lessons/my-active` | Все активные курсы пользователя (M-карточки) | те же эндпоинты, агрегация на FE |

**Что убираем из текущей структуры:**
- `MyCoursesBlock` (свои авторские курсы) — на главной заменяется
  одной строкой-врезкой «+ Создать свой курс» под L4. Полный список
  своих курсов переезжает в `/lessons/my-active` (отфильтрованный по
  `mine: true`) и в `/lessons/discover` (если public). Активный
  собственный курс (с прогрессом) попадает в Hero/`my-active` на
  общих основаниях.
- `EnrolledCoursesBlock` — на главной **удаляется**. Его контент
  целиком уходит в Hero (см. §3.2) и `/lessons/my-active`.
- `LevelGateBanner` — **удаляется полностью** (см. §5), без замены.
- `ReviewsDueBlock` (SM-2 повторения) **остаётся** на главной как
  отдельная компактная плашка над L2 (как сейчас, но визуально
  тоньше). Бейдж «N к повторению» на карточке активного курса —
  внутри Hero/`/lessons/my-active`.

---

### 4. Тизер курса — что в нём должно быть

Сейчас карточка — это «text card»: заголовок + описание + счётчик
уроков. Это работает для каталога из 50+ книг, где обложки путают,
но не работает для каталога из 5–10 курсов, где **главный конкурент
карточки — соседняя такая же карточка**.

#### 4.1 Визуальные элементы (по приоритету)

| Элемент | Зачем | Источник данных | Статус в коде |
|---|---|---|---|
| **Обложка** (1:1 или 16:9) | Главный визуальный «крючок». Обложка — это то, что отличает курс от соседа на расстоянии 2 метров от экрана | `coverUrl: string \| null` | **Нет в DTO** |
| **Заголовок** | — | `titleI18nKey` | Есть |
| **Метка уровня** | Beginner / Intermediate / Advanced | `level` | Есть |
| **Сложность внутри уровня** | 1–3 «звёздочки» или «easy/medium/hard» — внутри одного уровня курсы разной сложности | `difficulty: 1\|2\|3` | **Нет в DTO** |
| **Длительность** | «~ 2 часа», «4 урока» — помогает спланировать сессию | `lessonCount` (есть) + `estimatedMinutes: number` | Частично (только lessonCount) |
| **Целевая аудитория** | «Для играющих от 1200», «Для детей 6+», «После курса „Открытия"» | `audienceI18nKey: string \| null` | **Нет в DTO** |
| **Прогресс** (для активных) | Полоса + счётчик «3/8 уроков» | `progress.lessonsCompleted` | Есть |
| **Бейдж результата** | «✓ Пройден», «3 урока к повторению» | `progress.completedAt`, агрегат от SM-2 | Частично |
| **Ключевые темы** (теги) | 2–3 тега «эндшпиль», «тактика», «короткие партии» — для скана | агрегат `lesson.blockKey` или новое поле `tags: string[]` | **Нет** (есть на уровне урока) |
| **Автор** (для community) | Аватар + nickname | автор из `UserEnrolledCourseDto` | Частично |

#### 4.2 Иерархия размеров (S/M/L)

Реюзаем идею ADR-031 §4.3, но **с обложками**:

- **L (large)** — Hero для P1-single (один активный курс), Hero для
  P2 (CTA «начни с beginner»). Обложка большая (16:9), заголовок крупно,
  CTA primary. **1 на экране максимум.**
- **M (medium)** — карточки в L1 «Моё обучение», L2 «Рекомендуем»,
  L3 «Системный курс». Обложка 4:3 сверху, под ней — заголовок,
  бейджи (уровень, длительность, аудитория), прогресс.
- **S (small)** — strip «Сообщество», горизонтальный scroll. Обложка
  4:3 (компактно), заголовок 2 строки, автор. Без описания, без
  прогресса.

#### 4.3 Mockup карточки M

```
┌──────────────────────────────┐
│                              │
│      [ОБЛОЖКА 4:3]           │
│                              │
├──────────────────────────────┤
│ ⓘ Beginner · ★★☆ · ~2 ч     │  ← бейджи метаданных
│                              │
│ Базовые маты двумя           │  ← заголовок (2 строки макс)
│ ладьями и ферзём             │
│                              │
│ Для тех, кто только          │  ← аудитория (1 строка)
│ начал играть                 │
│                              │
│ ▓▓▓▓░░░░  3/8 уроков         │  ← прогресс (если есть)
│                              │
│ #эндшпиль  #мат              │  ← теги (опционально)
│                              │
│ [▶ Продолжить]               │  ← CTA
└──────────────────────────────┘
```

Для **карточки без прогресса** CTA — «Начать», прогресс-полоса убрана,
вместо неё — длительность («4 урока · ~ 2 часа»).

Для **завершённого курса** — бейдж «✓ Пройден» поверх обложки,
CTA «Повторить» (ведёт в SM-2 повторения, если есть; иначе — на
страницу курса).

#### 4.4 Что нужно от бэка

Поля, которых нет в DTO `CourseListItem` / `Course`:

```ts
interface CourseMeta {
  coverUrl: string | null;          // путь до картинки (или иконка-fallback)
  difficulty: 1 | 2 | 3;            // 1=easy, 2=medium, 3=hard внутри уровня
  estimatedMinutes: number | null;  // оценка времени на весь курс
  audienceI18nKey: string | null;   // i18n-ключ описания ЦА
  tags: string[];                   // ['endgame', 'tactics']
}
```

**Миграция:** добавить колонки в `Course` (Prisma). Заполнить
системные курсы фикстурами в seed. Для пользовательских курсов —
обложка через upload, остальные поля — поля редактора курса.
Это **отдельная backend-задача**, ставится после согласования
концепции.

**Fallback при пустых данных:**
- Нет обложки → плейсхолдер с иконкой шахматной фигуры по уровню
  (пешка / конь / ферзь) + цветовой акцент по уровню.
- Нет `estimatedMinutes` → показываем только `lessonCount`.
- Нет `audienceI18nKey` → строка скрывается, не место не оставляется.
- Нет тегов → строка скрывается.

---

### 5. Блок «открытие уровня Средний» — удалить полностью

**Решение пользователя:** `LevelGateBanner` удаляется без замены.
Семантика уровней (что значит «средний», когда он «открывается») пока
не формализована — вернёмся отдельной задачей, когда будет реальный
Intermediate-контент.

#### 5.1 Почему удаляем

- Курсы Intermediate **не закрыты** — юзер может зайти в любой
  системный курс через `/lessons/<slug>`. Плашка только **советует**,
  но выглядит как блокер.
- Список блокеров («сыграно партий 3/20», «puzzle-рейтинг 1100/1400»)
  смешивает **обучение** и **игру** — у юзера нет времени, ему хочется
  именно сейчас изучить эндшпиль, а не идти играть 17 партий.
- Плашка появляется **только для beginner** (`restrictTo="beginner"`)
  и исчезает при первом же повышении уровня — юзеры со средним
  уровнем её вообще не видят, поэтому она **не образовательная**,
  а только «триггерная».
- Семантика уровней (что внутри Intermediate, как туда попадают) не
  формализована — преждевременно показывать UI «требования», когда
  модель ещё не зафиксирована.

#### 5.2 Что конкретно удаляется

- Компонент `apps/web/src/components/lessons/LevelGateBanner.tsx` —
  удаляется (вместе с тестами).
- Использование в `LessonsPage.tsx:113` и в `CoursePage` (если есть
  встроенный banner с `from`-параметром — проверить и убрать).
- На бэке: эндпоинт `GET /lessons/level-gate` и связанная логика
  расчёта блокеров (`apps/api/src/lessons` — найти `level-gate`
  обработчик, удалить вместе с DTO `LevelGateResponse`/`LevelGateBlocker`).
- В `packages/shared/src/types/lessons.ts` — типы `LevelGateResponse`,
  `LevelGateBlocker`, `LevelGateBlockerKind` удаляются.
- i18n-ключи `lessons.levelGate.*` (en, ru) — удаляются.

**Безопасность изменения:** удаление не ломает доступ к курсам — все
курсы как были доступны через прямые URL, так и остаются. Логика
`recommendedLevel` (которую плашка не использовала) — не трогаем.

#### 5.3 Что НЕ делаем сейчас

- Не делаем горизонтальный level-progress таймлайн (отвергнут
  пользователем). Семантика уровней зафиксируется отдельной задачей.
- Не переделываем рейтинговые блокеры в «рекомендации» — `recommendedLevel`
  уже работает по `puzzle_rating`, дополнительная переделка не нужна.
- Не выносим «требования к уровню» в Профиль/Прогресс. Если потом
  понадобится — отдельная задача со своим обсуждением.

---

### 6. Тексты тизеров — структура

Сейчас в seed системного demo-курса:
- title: «Демо-курс (TextStep / PuzzleStep / QuizStep)»
- description: «Технический курс для dev/QA: по одному уроку на каждый
  реализованный тип шага плюс смешанный урок. Используется для
  визуальной проверки компонентов уроков.»

Это технические тексты для разработки. Для продакшна **тексты должны
переписываться вместе с появлением реальных курсов** — задача из
зоны chess-expert + редактуры.

#### 6.1 Шаблон описания курса (предложение)

Описание курса разбивается на **3 структурных блока** в DTO, каждый —
отдельный i18n-ключ:

```ts
interface CourseCopy {
  /** Hook — 1 предложение, что юзер научится делать */
  hookI18nKey: string;
  /** Audience — 1 строка «для кого этот курс» */
  audienceI18nKey: string;
  /** Outcome — 1 строка «что будет в финале» */
  outcomeI18nKey: string;
}
```

**Пример заполнения** (для гипотетического курса «Эндшпиль ладья
против короля»):

| Поле | Текст |
|---|---|
| title | «Эндшпиль: ладья против короля» |
| hook | «Научишься загонять одинокого короля в угол ладьёй и королём — приём, который выигрывает каждую вторую блиц-партию.» |
| audience | «Для играющих от 800 рейтинга, кто не знает базовых матов.» |
| outcome | «8 уроков, ~2 часа. После — 5 задач на проверку.» |

**Принципы написания:**

1. **Hook — глагол + результат, не процесс.** Не «изучаем эндшпиль»
   (процесс), а «научишься загонять короля» (результат + действие).
2. **Audience — конкретный признак, не пустые слова.** «Для
   начинающих» бесполезно. «Для играющих от 800 рейтинга, кто не
   знает базовых матов» — конкретно.
3. **Outcome — измеримый результат + объём.** Не «глубокое понимание
   эндшпиля», а «8 уроков, 2 часа, потом 5 задач».
4. **Никакого «маркетингового языка».** Слова «увлекательный»,
   «революционный», «уникальный» в шахматном контексте звучат как
   шум. Шахматистов мотивирует **конкретный навык** + **возможность
   сразу применить**.
5. **Глаголы 2-го лица единственного числа.** «Научишься», не
   «вы изучите». Личный тон, как от тренера.

#### 6.2 Шаблон для пользовательских курсов

Для авторских курсов (`MyCoursesBlock`) тексты пишет сам автор. В
редакторе курса добавить три отдельных поля (с placeholder'ом-шаблоном
выше) вместо одного `description`. **Это backend + редактор —
отдельная задача**, не входит в ADR концепции.

#### 6.3 Какие тексты пересмотреть в первую очередь

Сейчас в системном каталоге **только demo-курс** (`/project/apps/api/src/lessons/seed/courses/demo`).
Реальных системных курсов ещё нет. Это значит:

- **Тексты тизеров — задача chess-expert**, не frontend/архитектора.
  Создание реальных системных курсов и их описаний — отдельный поток
  работ, идёт параллельно.
- Шаблон выше — **для chess-expert**, чтобы при наполнении контента
  у курсов сразу была единая структура.

---

### 7. «До» и «после» — макет

#### 7.1 Текущая страница (анализ кода вместо скриншота)

```
┌─ /lessons (now) ─────────────────────────────────────────────────────┐
│                                                                       │
│  Уроки                                                                │
│  Структурированный курс шахмат                                        │
│                                                                       │
│  ┌─ HERO (1 курс выделен — почему?) ─────────────────────────────┐   │
│  │  Continue learning                                             │   │
│  │  Эндшпиль: пешечные структуры                                  │   │
│  │  ▓▓▓▓░░░░ 3/8 уроков                                          │   │
│  │  [Resume course]                                               │   │
│  └────────────────────────────────────────────────────────────────┘   │
│                                                                       │
│  К повторению сегодня · 2 урока [Повторить]                          │
│                                                                       │
│  ┌─ Чтобы открыть уровень «Средний» ─────────────────────────────┐   │
│  │  • Осталось уроков: 4                                          │   │  ← рудимент
│  │  • Puzzle-рейтинг: 1100/1400                                   │   │
│  │  • Сыграно партий: 3/20                                        │   │
│  └────────────────────────────────────────────────────────────────┘   │
│                                                                       │
│  Мои курсы                                  [+ Создать свой]         │
│  ┌──────┐ ┌──────┐                                                   │
│  │ text │ │ text │  ← карточки текстовые, без обложек, описание ""  │
│  └──────┘ └──────┘                                                   │
│                                                                       │
│  Курсы, которые я прохожу                                            │
│  ┌──────┐ ┌──────┐ ┌──────┐                                          │
│  │ text │ │ text │ │ text │                                          │
│  └──────┘ └──────┘ └──────┘                                          │
│                                                                       │
│  Структурированный курс шахмат                                       │
│  Beginner → Intermediate → Advanced                                   │
│  ─ Начинающий ──────────────────────────────────────────              │
│  ┌──────┐ ┌──────┐ ┌──────┐                                          │
│  │ text │ │ text │ │ text │                                          │
│  └──────┘ └──────┘ └──────┘                                          │
│  ─ Средний ─────────────────────────────────────────────              │
│  ...                                                                  │
│                                                                       │
│  ✦ Latest from community              [Browse all →]                 │
│  ┌──┐ ┌──┐ ┌──┐ ┌──┐ ┌──┐                                            │
│  └──┘ └──┘ └──┘ └──┘ └──┘                                            │
└───────────────────────────────────────────────────────────────────────┘
```

**Боль:**
- 8 секций, все одинаковой плотности и стиля.
- Hero «выделяет» курс, но юзер не понимает почему именно его.
- LevelGateBanner с numbers, но без bar'ов — выглядит как ошибка
  валидации, не как мотивация.
- Карточки везде однотипные — text-only, без обложек.

#### 7.2 Предлагаемая структура (mockup, 3 варианта Hero)

**Вариант A — 0 активных курсов (приветствие):**

```
┌─ /lessons (proposed, hero-A) ────────────────────────────────────────┐
│                                                                       │
│  ━━ HERO ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│                                                                       │
│  С возвращением, @user                                                │
│  Начни с курса для начинающих — это базовая программа Kingside.       │
│  ┌──────────────────────────────────┐                                 │
│  │ [Открыть курс для начинающих →]  │                                 │
│  └──────────────────────────────────┘                                 │
│                                                                       │
│  ━━ Reviews due (если есть) ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│  • 2 урока к повторению  [Повторить]                                  │
│                                                                       │
│  ━━ РЕКОМЕНДУЕМ ВАМ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│  ┌─ ОБЛОЖКА ─┐  ┌─ ОБЛОЖКА ─┐                                         │
│  │           │  │           │                                          │
│  ...                                                                  │
│                                                                       │
│  ━━ СИСТЕМНЫЙ КУРС ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│                                                                       │
│  Beginner → Intermediate → Advanced  (без таймлайна-индикатора)       │
│                                                                       │
│  ─ Начинающий (5 курсов) ─                                            │
│  [m-card] [m-card] [m-card] [m-card] [m-card]                         │
│                                                                       │
│  ─ Средний (8 курсов) ─                                               │
│  [m-card] [m-card] [m-card] ...                                       │
│                                                                       │
│  ━━ + Создать свой курс ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│                                                                       │
│  ━━ СООБЩЕСТВО ━━━━━━━━━━━━━━━━━━━━━━ [Browse all →] ━━━━━━━━━━━━  │
│  [s-card] [s-card] [s-card] [s-card] [s-card]                         │
└───────────────────────────────────────────────────────────────────────┘
```

**Вариант B — 1 активный курс (большая Continue-карточка):**

```
┌─ /lessons (proposed, hero-B) ────────────────────────────────────────┐
│                                                                       │
│  ━━ HERO ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│                                                                       │
│  ┌────────────────┐  Continue learning                                │
│  │                │  Эндшпиль: пешечные структуры                     │
│  │   board        │  Урок 5 из 8 — «Король и пешка против короля»    │
│  │   preview      │                                                    │
│  │                │  ▓▓▓▓░░░░ 4/8 уроков · 50%                       │
│  │ (mini-FEN)     │                                                    │
│  │                │  Последняя активность: 2 дня назад                │
│  │                │                                                    │
│  └────────────────┘  [▶ Продолжить]                                   │
│                                                                       │
│  ━━ Reviews due (если есть) ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│  ━━ РЕКОМЕНДУЕМ ВАМ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│  ━━ СИСТЕМНЫЙ КУРС ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│  ━━ + Создать свой курс ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│  ━━ СООБЩЕСТВО ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
└───────────────────────────────────────────────────────────────────────┘
```

**Вариант C — ≥2 активных курсов (compact-блок):**

```
┌─ /lessons (proposed, hero-C) ────────────────────────────────────────┐
│                                                                       │
│  ━━ HERO ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│                                                                       │
│  С возвращением, @user                                                │
│  У тебя 3 активных курса · последняя активность сегодня               │
│                                                                       │
│                              [Мои активные курсы →]                   │
│                              (ведёт на /lessons/my-active)            │
│                                                                       │
│  ━━ Reviews due (если есть) ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│  ━━ РЕКОМЕНДУЕМ ВАМ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│  ━━ СИСТЕМНЫЙ КУРС ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│  ━━ + Создать свой курс ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
│  ━━ СООБЩЕСТВО ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ │
└───────────────────────────────────────────────────────────────────────┘
```

**Страница `/lessons/my-active` (новая, для варианта C):**

```
┌─ /lessons/my-active ─────────────────────────────────────────────────┐
│  Уроки / Мои активные курсы                                          │
│                                                                       │
│  Мои активные курсы (3)                                               │
│  Сортировка: по последней активности                                  │
│                                                                       │
│  ┌─ ОБЛОЖКА ─┐  ┌─ ОБЛОЖКА ─┐  ┌─ ОБЛОЖКА ─┐                         │
│  │   board   │  │   board   │  │   board   │                          │
│  ├───────────┤  ├───────────┤  ├───────────┤                          │
│  │Beg·★★☆·2ч│  │Int·★☆☆·1ч│  │Adv·★★★·4ч│                          │
│  │Эндшпиль:  │  │Тактика:   │  │Сицилианка │                          │
│  │пешки      │  │связки     │  │           │                          │
│  │▓▓▓░ 3/8   │  │▓▓░░ 2/6   │  │▓░░░ 1/12  │                          │
│  │сегодня    │  │вчера      │  │неделю наз.│                          │
│  │[▶Продолжи]│  │[▶Продолжи]│  │[▶Продолжи]│                          │
│                                                                       │
│  [+ Создать свой курс]                                                │
│                                                                       │
│  ━━ СООБЩЕСТВО ━━━━━━━━━━━━━━━━━━━━━━ [Browse all →] ━━━━━━━━━━━━  │
│  ┌──┐┌──┐┌──┐┌──┐┌──┐                                                │
│  └──┘└──┘└──┘└──┘└──┘                                                │
└───────────────────────────────────────────────────────────────────────┘
```

**Что меняется:**
- Hero — **3 состояния** по числу активных курсов (A: 0 → приветствие;
  B: 1 → большая Continue-карточка с превью доски; C: ≥2 → compact со
  ссылкой на `/lessons/my-active`).
- Отдельной секции «Моё обучение» на главной **нет** — её роль
  выполняет Hero.
- Карточки **с обложками** (M-размер): обложка → метаданные (level,
  difficulty, длительность) → заголовок → audience → прогресс → CTA.
- **L2 «Рекомендуем»** — новая секция, 1–2 карточки. На MVP — через
  `recommendedLevel` (без полного recsys).
- L3 «Системный курс» — карточки с обложками, **без** таймлайна
  уровней. Уровни перечислены вертикально, как и сейчас.
- LevelGateBanner **удалён полностью** (без замены).
- `EnrolledCoursesBlock`, `MyCoursesBlock` как отдельные секции на
  главной **удаляются**. Их данные — в Hero / `/lessons/my-active`.
  CTA «+ Создать свой курс» остаётся одной строкой над L4.
- L4 «Сообщество» — без изменений (как в ADR-031).
- **Новая страница** `/lessons/my-active` — все активные курсы
  пользователя (вариант C переходит туда).

#### 7.3 Mobile

Принципы те же, что в ADR-031 §5.3:
- L2, L4 — **горизонтальный scroll** карточек.
- L3 — **вертикальный**, потому что это «main offer».
- Hero (любого варианта A/B/C) — на мобильном занимает ½–⅔ экрана.
  В варианте B превью доски и текст переходят в **stack** (board
  сверху, текст снизу).
- `/lessons/my-active` — карточки в один столбец (mobile) или
  2-колоночная сетка (desktop).

---

### 8. Что нужно от backend (зафиксировано)

#### 8.1 Расширение модели `Course` (полное)

Решение пользователя: добавляем все поля сразу, без поэтапного
пилота.

```prisma
model Course {
  // ... существующие поля
  coverUrl         String?   // путь к обложке (S3 / static)
  difficulty       Int       @default(2) // 1=easy, 2=medium, 3=hard внутри уровня
  estimatedMinutes Int?      // оценка времени на курс (минут)
  audienceI18nKey  String?   // i18n-ключ описания ЦА («для кого»)
  hookI18nKey      String?   // i18n-ключ hook'а («что научишься»)
  outcomeI18nKey   String?   // i18n-ключ результата («что в финале»)
  tags             String[]  @default([])  // ['endgame', 'tactics']
}
```

**Миграция:**
- Добавить колонки. Все nullable / с дефолтами — обратная совместимость
  не ломается.
- В seed-фикстурах (`apps/api/src/lessons/seed/courses/`) для существующих
  курсов поля **остаются пустыми** (демо-курс ничего не получает).
  Заполнение реальных курсов — отдельная задача автора курса через
  админку (вне зоны KS-1931).
- Старое `descriptionI18nKey` остаётся в модели как **fallback** для UI,
  если `hookI18nKey/outcomeI18nKey` пустые.

#### 8.2 DTO `CourseListItem` / `Course` — расширение

Поля выше пробрасываются в `CourseListItem`, `Course`,
`UserEnrolledCourseDto` (везде, где UI рендерит карточку курса).
В `packages/shared/src/types/lessons.ts` добавляются те же поля
в соответствующие интерфейсы.

#### 8.3 Удаление эндпоинта level-gate

- `GET /lessons/level-gate` — **удаляется полностью** (см. §5.2).
- Связанные DTO `LevelGateResponse`, `LevelGateBlocker`,
  `LevelGateBlockerKind` — удаляются.
- Логика расчёта блокеров на бэке — удаляется.
- i18n-ключи `lessons.levelGate.*` — удаляются.

#### 8.4 Эндпоинт рекомендаций (опционально)

- `GET /lessons/recommended` — 1–2 курса по уровню + слабые темы из
  puzzle-stats. **Не входит в MVP KS-1931** — на MVP фронт берёт
  `recommendedLevel` (уже есть в `CourseListResponse`) + случайный
  выбор из этого уровня. Полноценный recsys — отдельная задача.

#### 8.5 Эндпоинт `/lessons/active-courses` (опционально)

- Можно добавить для оптимизации (один запрос вместо двух
  `listEnrolled` + `listCourses`-фильтр). На MVP агрегация на FE
  допустима — данные те же.

---

### 9. Принятые решения (журнал)

| Развилка | Дата | Решение |
|---|---|---|
| 1. Hero для P1 | 2026-04-26 | **3-state по числу активных курсов**: 0 → приветствие; 1 → большая Continue-карточка; ≥2 → compact со ссылкой на `/lessons/my-active`. См. §3.2. |
| 2. LevelGateBanner | 2026-04-26 | **Удалить полностью**, без замены. Семантика уровней не формализована, вернёмся отдельно. См. §5. |
| 3. Расширение DTO Course | 2026-04-26 | **Полное расширение**: `coverUrl`, `difficulty`, `estimatedMinutes`, `audienceI18nKey`, `hookI18nKey`, `outcomeI18nKey`, `tags`. Заполнение контента — отдельная задача автора курса через админку. См. §8. |

---

### 10. Что НЕ входит в эту концепцию

- **Реализация (код)** — это документ-обсуждение. Задачи для
  frontend/backend/layout создаёт координатор после согласования.
- **Аналитика и метрики успеха** — задача marketing-агента (см.
  ADR-031 §8 — уже описаны базовые метрики).
- **Контент системных курсов** (тексты hook/audience/outcome для
  конкретных курсов, реальные обложки) — задача chess-expert и
  автора курса через админку. Концепция даёт только шаблон (§6.1).
- **Админка автора курса** для заполнения новых полей — отдельная
  задача (формы редактора, upload обложек). Прорабатывается отдельно.
- **Анимации и hover-эффекты** — зона layout, в задачах ниже
  упомянуты как часть стайлинга.
- **Discover-страница `/lessons/discover`** — не пересматриваем,
  она работает по ADR-031 §3.
- **Пересмотр семантики уровней** (Beginner/Intermediate/Advanced) —
  отдельная задача после удаления LevelGateBanner.

---

### 11. Разбивка на задачи (для координатора)

Разбивка по агентам с зависимостями. Сроки и приоритеты — зона
координатора. Метки в скобках — рекомендованные labels.

#### 11.1 Backend (агент `backend`)

| # | Задача | Описание | Зависит от | Метки |
|---|---|---|---|---|
| **B-1** | Расширение модели `Course` | Prisma migration: добавить колонки `coverUrl`, `difficulty`, `estimatedMinutes`, `audienceI18nKey`, `hookI18nKey`, `outcomeI18nKey`, `tags` (см. §8.1). Все nullable / с дефолтами. Старое `descriptionI18nKey` остаётся как fallback. | — | `prisma` |
| **B-2** | Обновление DTO в `packages/shared` | Добавить новые поля в `CourseListItem`, `Course`, `UserEnrolledCourseDto`. Сборка `@kingside/shared`. | B-1 |  |
| **B-3** | Проброс полей в API-ответах | Обновить `CoursesService.listCourses()`, `CoursesService.getCourse()`, `UserCoursesService.listEnrolled()` — мапить новые колонки в DTO. | B-1, B-2 |  |
| **B-4** | Удаление эндпоинта `/lessons/level-gate` | Удалить controller-метод, service-логику расчёта блокеров, DTO `LevelGateResponse`/`LevelGateBlocker`/`LevelGateBlockerKind` в `packages/shared`. Обновить тесты. | — |  |
| **B-5** | (опционально) `GET /lessons/active-courses` | Один запрос с агрегатом активных курсов (system + custom + enrolled). Не критично для MVP — можно делать на FE. Ставим, если FE-1 встретит проблемы. | B-3 |  |

**Замечание B-1:** seed-фикстуры демо-курса оставить с пустыми
новыми полями. Заполнение системных курсов — отдельная задача
chess-expert после релиза.

#### 11.2 Frontend (агент `frontend`)

| # | Задача | Описание | Зависит от | Метки |
|---|---|---|---|---|
| **F-1** | Hero 3-state | Расширить `useLessonsHeroContext` подсчётом активных курсов (`progress != null && completedAt == null`). Реализовать 3 варианта рендера в `LessonsHero.tsx`: A (приветствие), B (большая Continue-карточка), C (compact-блок со ссылкой). См. §3.2, §7.2. | B-2, B-3 | `onboarding` |
| **F-2** | Удалить `LevelGateBanner` | Убрать импорт и использование в `LessonsPage.tsx:113`. Удалить файлы `LevelGateBanner.tsx`, `LevelGateBanner.test.tsx`. Если используется в `CoursePage` — тоже убрать. Удалить `lessonsApi.getLevelGate()`. | B-4 |  |
| **F-3** | Удалить `EnrolledCoursesBlock` / `MyCoursesBlock` с главной | На главной эти блоки больше не рендерятся. Сами компоненты можно реюзать на `/lessons/my-active` либо удалить, если он использует другую вёрстку. CTA «+ Создать свой курс» → одна строка над L4. | F-1 |  |
| **F-4** | Новая страница `/lessons/my-active` | `MyActiveCoursesPage` — вертикальный/2-колоночный список M-карточек активных курсов (system + custom + enrolled), сортировка по `lastActivityAt` desc. Routing в `App.tsx`. Breadcrumb «Уроки / Мои активные курсы». См. §3.2. | F-1 |  |
| **F-5** | Новая M-карточка курса | Компонент `CourseCard` (M-размер): обложка 4:3 → бейджи (level / difficulty / длительность) → заголовок (2 строки) → audience (1 строка) → прогресс / CTA. Fallback'ы для пустых полей (см. §4.4). Реюзается во всех местах рендера курсов. | F-1 |  |
| **F-6** | Замена карточек в `CurriculumPillarBlock` | Заменить inline-вёрстку `lessons-course-card` на новый `CourseCard` (M). Сохранить логику `recommendedBadge`. | F-5 |  |
| **F-7** | Новая секция «Рекомендуем вам» | `RecommendedCoursesBlock` — 1–2 M-карточки по `recommendedLevel`. Показ только при наличии `recommendedLevel`. Лого-стрипить (lazy mount) — реюзать `LazySection`. | F-5 |  |
| **F-8** | Удалить таймлайн level-progress (если был запланирован) | В концепции 1-й итерации (§5.2 вариант B) был горизонтальный таймлайн в шапке L3. Этого делать **не нужно** — пользователь отверг. Просто убедиться, что новой вёрстки L3 не вводит таймлайна. | F-6 |  |
| **F-9** | i18n-ключи | Добавить `lessons.hero.welcome.*` (вариант A), `lessons.hero.continue.*` (вариант B, может уже есть), `lessons.hero.multipleActive.*` (вариант C), `lessons.myActive.*` (новая страница), `lessons.recommended.title` (en, ru). Удалить `lessons.levelGate.*`. | F-1, F-2, F-4, F-7 | `i18n` |
| **F-10** | Тесты vitest | Юнит-тесты на 3 состояния Hero, на `MyActiveCoursesPage`, на `RecommendedCoursesBlock`, на `CourseCard` (с/без обложки, прогресс, fallback'ы). Обновить `LessonsPage.test.tsx`. Проверить, что удаление `LevelGateBanner` не ломает соседние блоки. | F-1..F-7 | `tests` |

#### 11.3 Layout (агент `layout`)

| # | Задача | Описание | Зависит от | Метки |
|---|---|---|---|---|
| **L-1** | CSS для Hero вариантов A/B/C | Стили: A — приветствие центрированное; B — large card с превью доски слева, текстом справа (на mobile — stack); C — compact-блок с CTA. Skeleton-варианты. | F-1 |  |
| **L-2** | CSS для `CourseCard` (M-размер) | Сетка обложка → meta-row → title → audience → progress → CTA. Hover-эффекты (subtle elevation). Fallback стили для пустой обложки (плейсхолдер с иконкой по уровню). | F-5 |  |
| **L-3** | CSS для `/lessons/my-active` | Сетка карточек (1 колонка mobile / 2 колонки tablet / 3 колонки desktop). Header страницы. | F-4 |  |
| **L-4** | CSS для «Рекомендуем вам» | Заголовок-разделитель, 1–2 карточки в ряд. | F-7 |  |
| **L-5** | Mobile horizontal scroll | Для L2/L4 на mobile — horizontal scroll. Для L3 — оставить вертикальный. См. §7.3. | F-6, F-7 |  |
| **L-6** | Skeleton CSS (`@keyframes pulse`) | Если нет — добавить общую `.skeleton-pulse` анимацию для всех новых skeleton'ов. | F-1, F-5 |  |

#### 11.4 Зависимости — критический путь

```
B-1 ──► B-2 ──► B-3 ──┬─► F-1 ──► F-3 ──┐
                      │                  ├──► F-9 ──► F-10
                      ├─► F-5 ──► F-6 ───┤
                      │                  │
                      ├─► F-7 ───────────┘
                      │
B-4 ──► F-2 ──────────┘
                      
F-1 ──► F-4 ──► L-3
F-1 ──► L-1
F-5 ──► L-2
F-7 ──► L-4
F-6, F-7 ──► L-5
```

**Минимальный путь до видимого результата:** B-1 → B-2 → B-3 → F-1 → L-1
(работающий 3-state Hero без новых карточек). Карточки и `/my-active`
можно катить второй волной.

#### 11.5 Что НЕ требует backend-задачи

- Удаление `LevelGateBanner` на FE может идти параллельно с B-4
  (FE-2 не блокирует B-4 и наоборот). Но в один релиз — иначе FE
  будет дёргать удалённый эндпоинт.
- Создание `CourseCard` (F-5) можно начать на текущих DTO (старые
  поля), а после B-3 расширить. Это сократит критический путь, но
  усложнит ревью — на усмотрение координатора.

#### 11.6 Зона координатора / отдельные задачи (вне KS-1931)

- **Админка автора курса** для заполнения `coverUrl`/`hook`/`audience`/
  `outcome`/`tags`/`difficulty`/`estimatedMinutes`. Отдельная задача
  на frontend + backend (формы, валидация, upload обложек).
- **Контент системных курсов** — задача chess-expert: написать тексты
  по шаблону §6.1, выбрать обложки. После того как админка готова.
- **Удаление i18n-ключей `lessons.levelGate.*`** в обоих языках
  (en, ru) — может пойти вместе с F-2 или отдельной мелкой задачей.
- **Семантика уровней** (что внутри Intermediate, как туда попадают) —
  отдельная задача (продуктовое решение + дизайн).

---

### Приложение A. Соответствие пунктам ТЗ задачи

| Пункт ТЗ | Где в документе |
|---|---|
| 1. Структура страницы (мои/каталог/рекомендации; равноправие активных курсов) | §3 (4-уровневая группировка); §3.2 (Hero 3-state по числу активных) |
| 2. Тизер курса (визуальные элементы, иерархия) | §4 |
| 3. Блок «открытие уровня Средний» (удалить/заменить) | §5 — **удалить полностью** |
| 4. Тексты тизеров (структура, шаблон) | §6 |
| 5. Скриншоты «до» / mockup «после» | §7 (скрин «до» через playwright не сделан — `/project/node_modules` отсутствует в контейнере архитектора; скрины снимет frontend при реализации) |
| Решения по развилкам (3) | §0.1 (TL;DR) и §9 (журнал) |
| Разбивка на задачи с зависимостями | §11 |
