/**
 * Константы архивного индекса (ADR-014, ADR-016).
 */

/**
 * Максимальный ply, индексируемый в `position_stats` и
 * `archive_game_positions`. Единая константа — инвариант #2 (ply-lock)
 * из ADR-016: значения в обоих индексах должны совпадать, иначе дерево
 * ходов и список партий рассинхронизируются (партия видна как ход в
 * дереве, но пропадает из списка партий, использующих эту позицию).
 */
export const ARCHIVE_PLY_LIMIT = 40;
