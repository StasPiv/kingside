# Board-recog training requirements (KS-2361, ADR-040 Stage 2).
#
# These are the pinned versions used to produce the v1.0 ONNX model. They are
# intentionally narrow so that a Colab session reproduces the same artefact
# bit-for-bit. Update conservatively — torchvision moves the MobileNetV3-Small
# _mobilenet_v3_conf helper occasionally (see training/model.py).
#
# CUDA 12.1 wheels assumed (the default on Colab T4 / A100 as of 2026-05).
# For Lambda Labs or local CPU runs, swap `torch` for the matching wheel index
# at https://pytorch.org/get-started/locally/

torch==2.4.1
torchvision==0.19.1
numpy==1.26.4
Pillow==10.4.0
albumentations==1.4.18
opencv-python-headless==4.10.0.84
onnx==1.17.0
onnxruntime==1.19.2
scikit-learn==1.5.2
tqdm==4.66.5

# KS-3080 / runbook §9.7. HDF5 backend for the per-split datasets
# (cells_train.h5 / cells_val.h5 / cells_test.h5). Cuts cloud-dataset
# delivery from ~4.5 h (aws s3 sync v1/) to ~3 min (aws s3 cp v1-h5/).
h5py==3.11.0

# AWS CLI is only needed once at the end (upload the ONNX via
# tools/upload-board-model.sh). Install separately on the Colab host:
#   !pip install awscli
