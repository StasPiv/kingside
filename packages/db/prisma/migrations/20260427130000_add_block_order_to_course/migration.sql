-- KS-2037: упорядоченный список `blockKey` уроков курса.
-- Снимает хардкод `BLOCK_ORDER` с фронта — порядок блоков теперь
-- задаётся методически, на стороне БД, а курсы без значения
-- получают пустой массив (фронт сортирует по дефолту).

ALTER TABLE "courses"
    ADD COLUMN "block_order" TEXT[] NOT NULL DEFAULT ARRAY[]::TEXT[];
